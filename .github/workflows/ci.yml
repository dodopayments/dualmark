name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  TURBO_TELEMETRY_DISABLED: 1
  CI: true

jobs:
  build-test-typecheck:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Setup
        uses: ./.github/actions/setup

      - name: Build all packages
        run: bun run build

      - name: Run tests
        run: bun run test

      - name: Typecheck
        run: bun run typecheck

      - name: CLI smoke test
        run: |
          node packages/cli/dist/cli.js --help
          node packages/cli/dist/cli.js verify --help

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Deno example smoke test
        working-directory: examples/deno-deploy
        run: deno task test

      - name: Verify spec docs are in sync
        working-directory: apps/docs
        run: |
          bun run sync-spec
          if ! git diff --quiet content/docs/spec/; then
            echo "::error::apps/docs/content/docs/spec/ is out of sync with /spec/. Run 'bun run --filter dualmark-docs sync-spec' and commit." >&2
            git diff --stat content/docs/spec/ >&2
            exit 1
          fi
